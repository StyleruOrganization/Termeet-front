import styles from "./<FTName | capitalize>.module.css";
import type { <FTName | capitalize>Props } from "./<FTName | capitalize>.types";

export const <FTName | capitalize> = (props: <FTName | capitalize>Props) => {
  return (
    <div className={styles.<FTName | capitalize>}>
      
    </div>
  );
};