# Libs Structure

Директория `shared/libs` содержит переиспользуемый код, не привязанный к конкретной предметной области проекта. Это фундаментальные утилиты, хелперы и конфигурации, которые могут использоваться в любом месте приложения.

## 📁 Структура папок

### `/dates`

**Назначение**: Утилиты для работы с датами.

- `const.ts` - константы, связанные с датами (форматы, дни недели, месяцы и т.д.)

### `/times`

**Назначение**: Утилиты для работы со временем и константы.

- `const.ts` - константы, связанные с временем. 

### `/devices`

**Назначение**: Определение типов устройств и их возможностей.

- `isTouchDevice.ts` - детекция touch-устройств

### `/func`

**Назначение**: Функциональные утилиты высшего порядка.

- `functionWrappers.ts` - обертки для функций (debounce, throttle, once, и т.д.)

### `/react`

**Назначение**: React-специфичные утилиты и хуки.

- `mergeRefs.ts` - утилита для объединения нескольких ref в один

**Правила**:

- Только универсальные React-утилиты
- Хуки должны быть повторно используемыми в любых компонентах
- Каждый хук или утилита в отдельном файле

## 🚫 Чего НЕ должно быть в shared/libs

- Бизнес-логики
- Компонентов, специфичных для конкретного фич-модуля
- Запросов к API (только утилиты для работы с ними)
- Стилей и тем оформления